Create in this empty folder a python project that shows how to use wxwidgets to build a GUI application which contains:
 * a sidebar on the left with options / buttons with icons
 * a main screen that will change when sidebar options are clicked. In this main screen show sample of all widgets for forms
 * a menu with sub-menues at the top.

add a new option and page "table" which allow suser to select a .csv file and display it in a table. User can sort by or filter by columns. Make sure it supports large data

in screen "table", make sure all table columns are visible and that the user can resize horizontally each column